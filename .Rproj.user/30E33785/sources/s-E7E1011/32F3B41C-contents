# Rの使い方 DataFrame

# 1.dataframeの作成

df.empty <- data.frame() # 空のデータフレームの作成
df.score <- data.frame(name = c("tanaka","nakata","kimura"),english = c(60,40,88),math = c(47,68,59))

print(df.score)

df.empty <- rbindlist(list(df.empty,c("mob",1,2,3)))

print(df.empty)

